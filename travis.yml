os: linux
dist: xenial
language: python
python: 
    - "2.7"
    - "3.8"
services:
    - docker
install:
    - pip install -r requirements/dev.txt
    - cd web
    - yarn global add grunt-cli
    - yarn install
    - grunt
deploy:
    - docker-compose up --build